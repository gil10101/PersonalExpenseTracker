{
  "version": "2018-05-29",
  "statements": [
    ## Convert ISO8601 date to MySQL datetime format
    #set($isoDate = $context.arguments.date)
    #set($mysqlDate = $isoDate.replace('T', ' ').replace('Z', ''))
    "UPDATE expenses SET name = '$context.arguments.name', amount = $context.arguments.amount, category = '$context.arguments.category', date = '$mysqlDate' WHERE id = '$context.arguments.id'",
    "SELECT * FROM expenses WHERE id = '$context.arguments.id'"
  ]
}